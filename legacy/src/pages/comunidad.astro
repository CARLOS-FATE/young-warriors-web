---
// src/pages/comunidad.astro
import Layout from '../layouts/Layout.astro';

// Datos de la galería, organizados por evento.
// En el futuro, esto vendrá de la API.
const galleryEvents = [
  {
    name: 'Torneo de Verano 2025',
    photos: [
      { src: '/images/gallery/summer-1.jpg', alt: 'Jugador lanzando un tiro libre' },
      { src: '/images/gallery/summer-2.jpg', alt: 'Equipo celebrando una victoria' },
      { src: '/images/gallery/summer-3.jpg', alt: 'Entrenador dando instrucciones' },
      { src: '/images/gallery/summer-4.jpg', alt: 'Padres animando desde las gradas' },
    ]
  },
  {
    name: 'Día de Entrenamiento',
    photos: [
      { src: '/images/gallery/training-1.jpg', alt: 'Jugadores haciendo ejercicios de dribbling' },
      { src: '/images/gallery/training-2.jpg', alt: 'Jugadora practicando un crossover' },
      { src: '/images/gallery/training-3.jpg', alt: 'Estiramientos en equipo antes de empezar' },
    ]
  }
];
---

<Layout
  title="Nuestra Comunidad | Galería de Momentos Young Warriors"
  description="Explora los mejores momentos de Young Warriors Club. Mira las fotos de nuestros torneos, entrenamientos y eventos comunitarios en Trujillo."
>
  <section class="page-header">
    <div class="container-narrow">
      <span>Nuestra Familia</span>
      <h1>El Corazón del Club: Nuestra Gente</h1>
      <p>Revive los momentos que nos definen. Cada foto cuenta una historia de esfuerzo, compañerismo y pasión por el baloncesto.</p>
    </div>
  </section>

  <section class="gallery-section container">
    {galleryEvents.map(event => (
      <div class="event-album">
        <h2>{event.name}</h2>
        <div class="photo-grid">
          {event.photos.map(photo => (
            <div class="photo-card">
              <img src={photo.src} alt={photo.alt} loading="lazy" />
            </div>
          ))}
        </div>
      </div>
    ))}
  </section>

  <section class="coming-soon container">
    <h2>Más Historias por Contar</h2>
    <p>Próximamente aquí encontrarás testimonios de nuestros padres y jugadores, además de historias de éxito de nuestros ex-alumnos que hoy triunfan dentro y fuera de la cancha.</p>
  </section>

</Layout>

<style>
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 4rem 1.5rem;
  }
  .container-narrow {
    max-width: 800px;
    margin: 0 auto;
    padding: 4rem 1.5rem;
    text-align: center;
  }

  /* Estilos del Encabezado */
  .page-header {
    background-color: var(--color-bg-muted);
  }
  .page-header span {
    color: var(--color-primary);
    font-weight: 600;
  }
  .page-header h1 {
    font-size: 3rem;
    margin-top: 0.5rem;
  }
  .page-header p {
    margin-top: 1rem;
    font-size: 1.1rem;
    line-height: 1.7;
  }

  /* Estilos de la Galería */
  .event-album {
    margin-bottom: 4rem;
  }
  .event-album h2 {
    font-size: 2.5rem;
    border-bottom: 3px solid var(--color-primary);
    padding-bottom: 0.5rem;
    margin-bottom: 2rem;
    display: inline-block;
  }
  .photo-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 1.5rem;
  }
  .photo-card {
    border-radius: 0.75rem;
    overflow: hidden;
    box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }
  .photo-card:hover {
    transform: scale(1.03);
    box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1);
  }
  .photo-card img {
    width: 100%;
    height: 250px;
    object-fit: cover;
    display: block;
  }

  /* Estilos de la sección "Próximamente" */
  .coming-soon {
    text-align: center;
    background-color: var(--color-bg-muted);
    border-radius: 0.75rem;
  }
</style>