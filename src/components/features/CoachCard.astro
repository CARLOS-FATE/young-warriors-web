---
// src/components/features/CoachCard.astro
export interface Props {
  coach: {
    name: string;
    role: string;
    img: string;
    bio: string;
    quote: string;
  };
}
const { coach } = Astro.props;
---

<div class="card">
  <img src={coach.img} alt={`Foto de ${coach.name}`} loading="lazy" />
  <div class="card-content">
    <h3>{coach.name}</h3>
    <span class="role">{coach.role}</span>
    <p class="bio">{coach.bio}</p>
    <p class="quote">"{coach.quote}"</p>
  </div>
</div>

<style>
  .card {
    background-color: white;
    border-radius: 0.75rem;
    box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1);
    overflow: hidden;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }
  .card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1);
  }
  .card img {
    width: 100%;
    height: 300px;
    object-fit: cover;
    object-position: top; /* Asegura que la cara sea visible */
  }
  .card-content {
    padding: 1.5rem;
  }
  h3 {
    margin: 0;
    font-size: 1.5rem;
    color: #111827;
  }
  .role {
    color: #6366f1;
    font-weight: 600;
    display: block;
    margin-top: 0.25rem;
  }
  .bio {
    margin-top: 1rem;
    font-size: 0.9rem;
    line-height: 1.6;
    color: #4b5563;
  }
  .quote {
    margin-top: 1rem;
    font-style: italic;
    color: #6b7280;
    border-left: 3px solid #6366f1;
    padding-left: 1rem;
  }
</style>