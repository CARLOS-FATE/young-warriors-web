---
// src/pages/admin/crear-player.astro
import { checkAdminAuth } from "../../../lib/authAdmin.js"; // <-- ¡Usamos nuestro guardia!

// 1. Verificamos si el admin está logueado. Si no, esta función redirige.
checkAdminAuth(Astro);

// (El resto es solo el HTML)
---

<!doctype html>
<html lang="es">
    <head>
        <meta charset="UTF-8" />
        <title>Añadir Nuevo Jugador</title>
        <style>
            body {
                font-family: Arial, sans-serif;
                line-height: 1.6;
                margin: 20px;
            }
            h1 {
                color: #333;
            }
            form {
                max-width: 600px;
                margin-top: 20px;
            }
            .form-group {
                margin-bottom: 15px;
            }
            /* ... (Copia el resto de tus estilos de crear_player.php) ... */
        </style>
    </head>
    <body>
        <h1>Añadir Nuevo Jugador</h1>

        <form action="/api/admin/crear-player" method="post">
            <div class="form-group">
                <label for="name">Nombre:</label>
                <input type="text" id="name" name="name" required />
            </div>
            <button type="submit">Guardar Jugador</button>
        </form>

        <br />
        <a href="/admin/players">Volver a la lista</a>
    </body>
</html>
